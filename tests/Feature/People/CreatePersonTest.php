<?php

namespace Tests\Feature\People;

use App\Models\Person;
use Tests\TestCase;

class CreatePersonTest extends TestCase
{
    /**
     * @test
     */
    public function can_visited_a_page_home()
    {
        // $people = factory(Person::class)->times(3)->create()->dump();
        $this->assertTrue(true);
    }
}
